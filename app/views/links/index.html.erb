<h1>This is the index page for links</h1>

<% @links.each do |link| %>
  <p><%=image_tag(link.logo)%><%= link_to(link.name, link_path(link.id)) %></p>
<% end %>

<%= link_to("New link", new_link_path) %>